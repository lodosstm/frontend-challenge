import template from './detail.html';
import controller from './detail.controller';

export default {
  restrict: 'E',
  scope: {},
  template,
  controller,
  controllerAs: 'dt',
  bindToController: true
};
